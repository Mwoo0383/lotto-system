<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.lotto.repository.ParticipantMapper">

    <insert id="insertParticipant" parameterType="Participant" useGeneratedKeys="true" keyProperty="participantId">
        INSERT INTO participant (
        phone_hash,
        phone_encrypted,
        phone_last4,
        ticket_seq,
        created_at,
        event_id
        )
        VALUES (
        #{phoneHash},
        #{phoneEncrypted},
        #{phoneLast4},
        #{ticketSeq},
        NOW(),
        #{eventId}
        )
    </insert>

    <select id="findByPhoneHashAndEventId" resultType="Participant">
        SELECT *
        FROM participant
        WHERE phone_hash = #{phoneHash}
        AND event_id = #{eventId}
    </select>

    <select id="countByEventId" resultType="int">
        SELECT COUNT(*)
        FROM participant
        WHERE event_id = #{eventId}
    </select>

</mapper>